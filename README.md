# Glide

Glide is a toolkit for building wallet-enabled web games for the Godot game engine.

Current status: installable Godot addon skeleton with a visible `Glide Web3` bottom panel, shell template files, setup validation including export template checks, managed preset discovery, real Web export invocation, and managed custom shell hookup.

Next checkpoint: stabilize Slice 1 and confirm wrapped export works in a clean project.
