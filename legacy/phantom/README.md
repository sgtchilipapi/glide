## Legacy Phantom Integration

This folder archives the previous Phantom-specific shell provider path.

Phantom is no longer the active Glide auth provider. Glide now uses Privy for the active embedded auth flow.

Archived source:
- `web-shell/src/phantom.ts`

Notes:
- Historical Phantom references may still exist in older implementation-plan sections for project history.
- Active docs and active shell/runtime code should point to Privy instead.
